#pragma once
#pragma once
#include "../core/reflection-macro.hpp"
namespace lighten::render::components
{
    // TODO
/*    LIGHTEN COMPONENT(name = "Model", category = "Core")
    struct Mesh
    {
    public:
        uint32_t model_id;
        uint32_t mesh_id;

        uint32_t material_instance_id;
    };

    LIGHTEN COMPONENT(name = "Model", category = "Core")
    struct MeshArray
    {
    public:
        std::vector<Mesh> meshes;
    };
    */
} // namespace lighten::components